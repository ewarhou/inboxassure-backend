from spamcheck.models import UserSpamcheckBison; from analytics.signals import update_bison_campaigns_table; import django; import os; os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'settings.settings'); django.setup(); spamcheck = UserSpamcheckBison.objects.filter(status='completed').order_by('-updated_at').first(); print(f'Found spamcheck: {spamcheck.id} - {spamcheck.name}' if spamcheck else 'No completed spamchecks found'); update_bison_campaigns_table(spamcheck) if spamcheck else None; print('Update triggered successfully' if spamcheck else '')
